# AGENTS.md

This is a static HTML landing page project with no build system.

## Project Structure

- `index.html` - Single-file landing page (SupplyFlow IT consulting business)

## How to Work

- No build commands needed - edit `index.html` directly
- Open `index.html` in a browser to preview changes
- No tests, lint, or typecheck commands exist
- No package manager (npm/pip/etc) or dependency files

## Notes

- Plain HTML/CSS/JS - no frameworks
- Form submits to console (no backend)
- Git repository initialized
